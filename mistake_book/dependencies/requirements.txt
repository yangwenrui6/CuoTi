# 基础依赖
PyQt6>=6.4.0
SQLAlchemy>=2.0.0
platformdirs>=3.0.0
Pillow>=10.0.0
plyer>=2.1.0

# OCR引擎（可选 - 至少安装一个）
# 推荐: PaddleOCR (中文识别效果好)
# paddleocr>=2.7.0
# paddlepaddle>=2.5.0  # PaddleOCR的依赖

# 备选: Tesseract (开源免费)
# pytesseract>=0.3.10
# 注意: 需要单独安装Tesseract程序
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
# Linux: sudo apt-get install tesseract-ocr tesseract-ocr-chi-sim
# Mac: brew install tesseract tesseract-lang

# 导出功能（可选）
# reportlab>=4.0.0
# openpyxl>=3.1.0
